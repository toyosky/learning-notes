---
tags: [量化, 数学, 风险平价, 推导, 资产配置]
date: 2026-06-15
---

# 风险平价（Risk Parity）数学推导

> **写给谁**：有概率论和高等数学知识，但第一次接触量化组合数学的读者。
> **阅读目标**：把"波动率倒数加权"这个结论，从最朴素的直觉一步一步推出来。

---

> 💡 **分篇阅读**：本笔记已拆分为系列文章，可按主题精读：
> - [[risk-parity-intuition|直觉篇（§0-1）]] — 直觉实验 + 组合方差
> - [[risk-parity-decomposition|分解篇（§2-3）]] — MRC 与 RC 的数学分解 
> - [[risk-parity-solution|求解篇（§4-9）]] — 风险平价条件 + 数值求解 + 风险预算
>
> 本文为完整合并版，适合一次性通读全文脉络。

## 目录

- [0. 一个直觉实验](#0-一个直觉实验)
- [1. 组合波动率的数学工具](#1-组合波动率的数学工具)
- [2. 边际风险贡献（MRC）](#2-边际风险贡献mrc)
- [3. 总风险贡献（RC）](#3-总风险贡献rc)
- [4. 风险平价条件](#4-风险平价条件)
- [5. 两资产特例：封闭解](#5-两资产特例封闭解)
- [6. 为什么一般情况没有解析解](#6-为什么一般情况没有解析解)
- [7. 数值求解直觉](#7-数值求解直觉)
- [8. 回头看：波动率倒数是零阶近似](#8-回头看波动率倒数是零阶近似)
- [9. 延伸：风险预算](#9-延伸风险预算)
- [附录：向量/矩阵速查](#附录-向量矩阵速查)

---

## 0. 一个直觉实验

假设你有两个资产：

| 资产 | 年化波动率 |
|------|-----------|
| 沪深300 | 18% |
| 国债 | 3% |

**等权各 50%**——谁的风险大？

直觉上，沪深300波动大，组合的风险应该主要来自它。但"等权"没有体现这一点：它给了高风险和低风险资产相同权重。

> **等权 = 等资金，不等于等风险。**

这引出一个关键问题：**能不能让每个资产对组合风险的"贡献"相等？**
这就是风险平价的出发点。

---

## 1. 组合波动率的数学工具

### 1.1 从方差开始

如果你只有一只资产 $i$，它的方差是 $\sigma_i^2$。但如果你持有一个组合 $P = w_1 A_1 + w_2 A_2 + \cdots + w_n A_n$，组合的方差不是简单的加权和——因为资产一起波动（协方差）。

组合方差的标准公式是：

$$
\sigma_p^2 = \operatorname{Var}(w^\mathsf{T} R) = w^\mathsf{T} \Sigma w
$$

其中 $\Sigma$ 是 $n \times n$ 协方差矩阵，$\Sigma_{ij} = \sigma_i \sigma_j \rho_{ij}$。

展开这个矩阵乘法：

$$
\sigma_p^2 = \sum_{i=1}^{n} \sum_{j=1}^{n} w_i w_j \sigma_i \sigma_j \rho_{ij}
$$

### 1.2 拆开来看

把双重求和展开成三部分：

$$
\sigma_p^2 = \underbrace{\sum_{i=1}^{n} w_i^2 \sigma_i^2}_{\text{自身方差}}
+ 2 \underbrace{\sum_{i < j} w_i w_j \sigma_i \sigma_j \rho_{ij}}_{\text{交叉协方差}}
$$

**意义**：
- 第一项：每只资产自己的波动
- 第二项：资产之间的联动（协方差）
- 如果 $\rho_{ij} > 0$（正相关），组合方差增大；如果 $\rho_{ij} < 0$（负相关），组合方差减小

这个展开在后面理解"风险贡献"时非常关键。

### 1.3 为什么不用方差而用波动率？

方差是平方量纲（%²），不好解释。组合的**标准差**（波动率）

$$
\sigma_p = \sqrt{w^\mathsf{T} \Sigma w}
$$

才是"组合涨跌的典型幅度"，量纲是 %，可以直接跟收益率比较。

> 后面推导风险贡献时，我们会在 $\sigma_p$（不是 $\sigma_p^2$）上做微分。这是关键细节。

---

## 2. 边际风险贡献（MRC）

### 2.1 核心问题

如果我给资产 $i$ 增加一丁点权重（从 $w_i$ 增加到 $w_i + \Delta$），组合波动率 $\sigma_p$ 会变多少？

这就是**边际风险贡献**（Marginal Risk Contribution, MRC）：

$$
\operatorname{MRC}_i = \frac{\partial \sigma_p}{\partial w_i}
$$

它回答的问题是：**"组合波动率对第 i 个权重的敏感度是多少？"**

### 2.2 推导

我们从 $\sigma_p = \sqrt{w^\mathsf{T} \Sigma w}$ 出发。对 $w_i$ 求偏导，用到两个知识点：

1. 链式法则：$\frac{\partial}{\partial w_i} \sqrt{f} = \frac{1}{2\sqrt{f}} \cdot \frac{\partial f}{\partial w_i}$
2. 二次型求导：$\frac{\partial}{\partial w_i} (w^\mathsf{T} \Sigma w) = 2 (\Sigma w)_i$

其中 $(\Sigma w)_i$ 是向量 $\Sigma w$ 的第 $i$ 个分量，展开为：

$$
(\Sigma w)_i = \sum_{j=1}^{n} w_j \sigma_i \sigma_j \rho_{ij}
$$

结合起来：

$$
\operatorname{MRC}_i = \frac{\partial \sigma_p}{\partial w_i}
= \frac{1}{2\sigma_p} \cdot 2 (\Sigma w)_i
= \frac{(\Sigma w)_i}{\sigma_p}
$$

展开分子：

$$
\operatorname{MRC}_i = \frac{\sum_{j=1}^{n} w_j \sigma_i \sigma_j \rho_{ij}}{\sigma_p}
$$

### 2.3 直观理解

- MRC 大的资产 → "增加它的权重会显著提高组合波动"
- MRC 小的资产 → "增加它的权重对组合波动影响不大"

**MRC 的核心驱动因素**：
1. **自身波动率 $\sigma_i$**：波动越大的资产，边际影响越大
2. **与组合的关联度**：$(\Sigma w)_i$ 包含了该资产与所有其他资产的协方差加权和

换句话说，MRC 不只是看单只资产的波动，还看它跟组合中其他资产如何互动。

---

## 3. 总风险贡献（RC）

### 3.1 定义

边际风险贡献回答的是"增加一单位权重的影响"，但资产 $i$ 当前已经有一个权重 $w_i$ 了。
**它实际贡献了多少风险？**

总风险贡献（Risk Contribution, RC）的定义很自然：

$$
\operatorname{RC}_i = w_i \cdot \operatorname{MRC}_i = w_i \cdot \frac{(\Sigma w)_i}{\sigma_p}
$$

### 3.2 为什么要乘 $w_i$？

考虑一个极端情况：如果 $w_i = 0$（我没买这只资产），那它对组合的风险贡献应该是 0，无论它的 MRC 多大。乘 $w_i$ 就保证了这一点。

另一个极端：如果 $w_i$ 很大，即使 MRC 不大，它占的"风险份额"也可能很大。

### 3.3 关键性质：RC 之和 = 组合波动率

这里有一个非常漂亮的数学性质——**欧拉齐次函数定理(Euler's Homogeneous Function Theorem)**：

$$
\sum_{i=1}^{n} \operatorname{RC}_i = \sum_{i=1}^{n} w_i \cdot \frac{\partial \sigma_p}{\partial w_i} = \sigma_p
$$

验证一下：

$$
\sum_{i=1}^{n} w_i \cdot \frac{(\Sigma w)_i}{\sigma_p}
= \frac{1}{\sigma_p} \sum_{i=1}^{n} w_i (\Sigma w)_i
= \frac{1}{\sigma_p} \cdot w^\mathsf{T} \Sigma w
= \frac{1}{\sigma_p} \cdot \sigma_p^2
= \sigma_p
$$

**这个性质为什么重要？**

因为 $\sigma_p$ 是"组合总风险"，现在我们可以说：

$$
\sigma_p = \operatorname{RC}_1 + \operatorname{RC}_2 + \cdots + \operatorname{RC}_n
$$

组合总波动率 = 各资产风险贡献的总和。

**数值例子**：假设 $\sigma_p = 15\%$，三只资产的 RC 分别是 5%、5%、5% → 风险平价！
但如果 RC 是 12%、2%、1% → 第一只资产主导了组合风险，"等权"实际上是"不等风险"。

---

## 4. 风险平价条件

### 4.1 核心等式

风险平价的定义就是：**让每只资产的风险贡献相等**。

$$
\operatorname{RC}_1 = \operatorname{RC}_2 = \cdots = \operatorname{RC}_n
$$

代入 RC 的定义：

$$
\frac{w_1 (\Sigma w)_1}{\sigma_p} = \frac{w_2 (\Sigma w)_2}{\sigma_p} = \cdots = \frac{w_n (\Sigma w)_n}{\sigma_p}
$$

消去所有分母的 $\sigma_p$：

$$
w_1 (\Sigma w)_1 = w_2 (\Sigma w)_2 = \cdots = w_n (\Sigma w)_n
$$

加上权重之和为 1 的约束 $\sum w_i = 1$，这就是风险平价的完整数学定义。

### 4.2 这为什么是一个非线性方程组？

展开 $w_1 (\Sigma w)_1$：

$$
w_1 (\Sigma w)_1 = w_1^2 \sigma_1^2 + w_1 w_2 \sigma_1 \sigma_2 \rho_{12} + \cdots + w_1 w_n \sigma_1 \sigma_n \rho_{1n}
$$

这里有 $w_1^2$（二次项），还有 $w_1 w_j$（交叉项）。当我们让 $w_1 (\Sigma w)_1 = w_2 (\Sigma w)_2$ 时，这个等式两边都含有 **所有** 的 $w$。

换句话说：**你不能独立地确定每个 $w_i$，因为每个等式都纠缠了所有变量。** 这就是为什么风险平价一般没有封闭解。

> **例外**：当所有资产互不相关（$\rho_{ij} = 0$）时，交叉项消失，问题大幅简化。我们在下个章节推导。

### 4.3 几何直觉

想象你在平衡一个多杆天平：
- 每根杆（资产）的长度不同（波动率不同）
- 杆之间的角度不同（相关性不同）
- 你需要移动配重（调整权重）让整个系统平衡

当杆之间有角度（相关性），移动一根杆会影响其他所有杆的力矩——这就是耦合（coupling）。

---

## 5. 两资产特例：封闭解

### 5.1 一般情况

两个资产，权重 $w_1$ 和 $w_2 = 1 - w_1$，相关性 $\rho$。

组合波动率：

$$
\sigma_p = \sqrt{w_1^2 \sigma_1^2 + w_2^2 \sigma_2^2 + 2 w_1 w_2 \sigma_1 \sigma_2 \rho}
$$

风险贡献：

$$
\operatorname{RC}_1 = \frac{w_1 (w_1 \sigma_1^2 + w_2 \sigma_1 \sigma_2 \rho)}{\sigma_p}
$$

$$
\operatorname{RC}_2 = \frac{w_2 (w_2 \sigma_2^2 + w_1 \sigma_1 \sigma_2 \rho)}{\sigma_p}
$$

令 $\operatorname{RC}_1 = \operatorname{RC}_2$，消去 $\sigma_p$：

$$
w_1 (w_1 \sigma_1^2 + w_2 \sigma_1 \sigma_2 \rho) = w_2 (w_2 \sigma_2^2 + w_1 \sigma_1 \sigma_2 \rho)
$$

代入 $w_2 = 1 - w_1$，得到一个关于 $w_1$ 的方程。当 $\rho \neq 0$ 时，它是一个二次方程——有解，但形式复杂，不如直接数值求解。

### 5.2 不相关的情况（$\rho = 0$）

当 $\rho = 0$ 时，交叉项消失：

$$
w_1^2 \sigma_1^2 = w_2^2 \sigma_2^2
$$

两边开方：

$$
w_1 \sigma_1 = w_2 \sigma_2
$$

代入 $w_2 = 1 - w_1$：

$$
w_1 \sigma_1 = (1 - w_1) \sigma_2
$$

解得：

$$
w_1 = \frac{\sigma_2}{\sigma_1 + \sigma_2}, \quad w_2 = \frac{\sigma_1}{\sigma_1 + \sigma_2}
$$

或者写成比例形式：

$$
\frac{w_1}{w_2} = \frac{\sigma_2}{\sigma_1}
$$

即：

$$
w_i \propto \frac{1}{\sigma_i}
$$

### 5.3 这就是"波动率倒数加权"

让我们验证一下这个解的经济直觉：

| 资产 | $\sigma$ | 风险平价权重 | 等权 |
|------|----------|------------|------|
| 沪深300 | 18% | $\frac{3}{18+3}=14.3\%$ | 50% |
| 国债 | 3% | $\frac{18}{18+3}=85.7\%$ | 50% |

**解读**：
- 等权（50/50）下，沪深300的 RC 远大于国债，组合风险被沪深300主导
- 风险平价下，国债配了 85.7%，沪深300只配了 14.3%
- 这样两者的风险贡献相等

**国债配到 85.7% 会不会太多？**
不——因为风险平价只关心风险贡献相等，不关心资金分配。
85.7% × 3% ≈ 14.3% × 18%，两者波动量相当。

> 这是风险平价最核心也是最反直觉的特点：**高权重 ≠ 高风险**。

---

## 6. 为什么一般情况没有解析解

### 6.1 耦合的来源

回到一般情况下的等式：

$$
w_1 (\Sigma w)_1 = w_2 (\Sigma w)_2 = \cdots = w_n (\Sigma w)_n
$$

展开来看，每个等式左边实际是：

$$
w_i \left( \sum_{j=1}^{n} w_j \sigma_i \sigma_j \rho_{ij} \right)
$$

这里 $\rho_{ij} \neq 0$ 意味着：
- $w_i$ 的波动不仅取决于自身（$\sigma_i$），还取决于它与其他所有资产的关系
- 改变 $w_1$ 会影响 $w_2$ 的等式，改变 $w_2$ 又反过来影响 $w_1$ 的等式

这是一个**耦合的非线性方程组**。

### 6.2 与线性方程组的区别

线性方程组（如 $Ax = b$）有成熟的闭式解（矩阵求逆）。但这里：

1. **二次项**：$w_i^2 \sigma_i^2$ 来自自身方差
2. **交叉项**：$w_i w_j \sigma_i \sigma_j \rho_{ij}$ 来自协方差
3. **耦合**：每个方程包含所有变量

所以一般情况需要数值求解。

### 6.3 现实意义

**当 $\rho$ 很大时（如两个股票型 ETF），简化版 $w \propto 1/\sigma$ 会给出严重误导。**

例如沪深300（$\sigma=18\%$）和中证500（$\sigma=20\%$），两者 $\rho \approx 0.8$。

- $w \propto 1/\sigma$ 版本：$w_{300} \approx 52.6\%, w_{500} \approx 47.4\%$
- 考虑 $\rho=0.8$ 的真实版本：$w_{300}$ 会更少，$w_{500}$ 会更多（因为两者高度同向波动，一个的"风险贡献"已经包含了另一个）

**直观理解**：两只高度正相关的资产本质上在"分担"同一个风险源。在你分配风险预算时，应该把它们视为**部分重复**的风险暴露，不能独立看待。

---

## 7. 数值求解直觉

### 7.1 优化问题的形式

风险平价问题可以写成一个最小化问题：

$$
\min_{w} \sum_{i=1}^{n} \sum_{j=1}^{n} \left( w_i (\Sigma w)_i - w_j (\Sigma w)_j \right)^2
$$

约束：

$$
\sum_{i=1}^{n} w_i = 1, \quad w_i \geq 0
$$

也就是：**找一组权重，使得所有资产的 RC 两两之间差异的平方和最小**。

### 7.2 为什么不用直接解方程？

因为超过 2 个资产时，方程个数多于自由参数，方程组是**过定的**。你无法让所有 RC 严格相等（或者解不存在，或者解超出 $[0,1]$ 范围），只能最小化它们之间的差异。

> 实践中"严格相等"往往不可行，所以风险平价在 3+ 资产时是"尽量接近相等"，而不是严格相等。

### 7.3 常用算法

1. **牛顿法**（Newton's Method）：利用梯度和 Hessian 矩阵迭代逼近，收敛快但计算 Hessian 复杂
2. **SQP**（Sequential Quadratic Programming）：把非线性约束逐步线性化，每步解一个二次规划
3. **Cyclical Coordinate Descent**：轮流固定其他变量，解一个变量的方程——实现简单，对风险平价问题特别有效

当前代码中的简化实现（`_risk_parity()` 方法）没有用这些数值方法——它走的是 $\rho=0$ 的简化。下一节解释原因。

---

## 8. 回头看：波动率倒数是零阶近似

### 8.1 我们的简化实现做了什么

```python
inv_vols[symbol] = 1.0 / vol
total = sum(inv_vols.values())
return {symbol: iv / total for symbol, iv in inv_vols.items()}
```

等价于解 $\rho=0$ 时的风险平价。这在数学上是**零阶近似**。

### 8.2 代价是什么

1. **正的 $\rho$ 被忽略**：正相关资产的风险被低估 → 配的权重过多
2. **负的 $\rho$ 被忽略**：负相关资产的对冲效果不被利用 → 配的权重过少
3. **RC 不再相等**：简化解算出的权重代入完整 RC 公式后，RC 不相等

### 8.3 为什么实践中还是用这个简化

1. **协方差的估计误差更大**：估计 $\sigma_i$ 已经不容易（需要 20-60 个交易日），估计 $\rho_{ij}$ 需要的样本量是平方级的。用一个误差很大的 $\rho$ 做数值优化，很可能不如直接用 $\rho=0$ 的简化解。
2. **数值稳定性**：迭代求解可能不收敛或在边界震荡
3. **简洁性**：一个除法就出结果，容易解释、容易调试

> 这是量化中常见的权衡：**估计偏差（bias）vs 估计方差（variance）**。
> 带 $\rho$ 的全解更精确（低偏差），但 $\rho$ 本身估计不准（高方差），最终效果可能还不如简化版。

---

## 9. 延伸：风险预算

### 9.1 从"平等"到"预算"

风险平价是"每只资产拿相等的风险预算"。但有些时候你不想平等——比如你更看好某个行业，愿意给它多分配一些风险。

**风险预算**（Risk Budgeting）是风险平价的推广：不是让 RC 相等，而是让 RC 等于预设的比例 $b_i$（满足 $\sum b_i = 1$）：

$$
\frac{\operatorname{RC}_i}{\sigma_p} = b_i
$$

即：

$$
\frac{w_i (\Sigma w)_i}{\sigma_p^2} = b_i
$$

### 9.2 风险平价是风险预算的特例

风险平价就是 $b_i = 1/n$（每只资产拿相同的风险预算）。

### 9.3 自由度对比

| 方法 | 参数 | 自由度 |
|------|------|--------|
| 等权 | 无 | 0 |
| 风险平价 | 目标: RC 相等 | 0（由 $\Sigma$ 决定） |
| 风险预算 | 比例 $b_i$ | $n-1$ |
| 均值-方差 | 收益率预期 $\mu$ + $\Sigma$ | $2n-1$ |

自由度从低到高，灵活性和对输入误差的敏感性同步增加。

---

## 附录：向量/矩阵速查

本文用到的线性代数知识（如果你不熟悉的话）：

| 符号 | 含义 |
|------|------|
| $w = (w_1, \dots, w_n)^\mathsf{T}$ | 权重向量（列向量） |
| $w^\mathsf{T}$ | 转置（行向量） |
| $\Sigma$ | 协方差矩阵，$n \times n$ |
| $\Sigma_{ij} = \sigma_i \sigma_j \rho_{ij}$ | 矩阵元素 |
| $w^\mathsf{T} \Sigma w$ | 二次型，结果为标量 |
| $(\Sigma w)_i$ | 向量 $\Sigma w$ 的第 $i$ 个分量 |
| $\frac{\partial}{\partial w} (w^\mathsf{T} \Sigma w) = 2\Sigma w$ | 二次型求导（列向量形式） |

---

## 相关笔记

### 分篇阅读

- [[risk-parity-intuition|直觉篇：等权 ≠ 等风险]] — 仅看直觉 + 组合方差（§0-1）
- [[risk-parity-decomposition|分解篇：风险贡献的数学分解]] — 仅看 MRC + RC（§2-3）
- [[risk-parity-solution|求解篇：风险平价条件与求解]] — 仅看条件 + 求解 + 推广（§4-9）

### 关联笔记

- [[../notes/portfolio-allocation|组合分配：等权 vs 风险平价 vs 动量排名]] — 代码实现 + 回测结果
- [[../notes/dca-backtest|DCA 定投回测]] — 前置概念：简单策略
- [前复权 vs 后复权](/2-quant-trading/01-data/deep/forward-vs-backward-adjustment) — 数据清洗基础
